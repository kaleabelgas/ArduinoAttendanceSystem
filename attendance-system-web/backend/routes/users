const express = require('express')

const router = express.Router()

// GET ALL
router.get('/', () => {

})

// GET ONE
router.get('/:id', () => {

})

// REGISTER USER
router.post('/:id', () => {

})

// EDIT USER
router.patch('/:id', () => {

})

module.exports = router;